extend type Query {
  me: Me
  user(userID: UUID!): User
}

extend type Mutation {
  updateMe(name: String, bio: String): Me!
}

type User {
  userID: ID!
  username: String
  name: String!
  bio: String
  photoURL: String
  createdOn: Time!
  projects: [Project!]!
}

type Me {
  userID: ID!
  user: User!
  email: String!
  updatedOn: Time!
}
