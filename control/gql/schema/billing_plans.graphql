extend type Query {
  billingPlans: [BillingPlan!]!
}

type BillingPlan {
  billingPlanID: UUID!
	default: Boolean!
	description: String
	currency: String!
	period: String!
	seatPriceCents: Int!
	seatReadQuota: Int!
	seatWriteQuota: Int!
	readOveragePriceCents: Int! 
	writeOveragePriceCents: Int!
	baseReadQuota: Int!
	baseWriteQuota: Int!
	readQuota: Int!
	writeQuota: Int!
	availableInUI: Boolean!
}
